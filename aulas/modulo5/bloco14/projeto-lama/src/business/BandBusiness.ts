import { BandInputDTO } from "../model/Band";

export class BandBusiness {

    async createBand(band: BandInputDTO) {
        try {
            
        } catch (error:any) {
            
        }
    }

}